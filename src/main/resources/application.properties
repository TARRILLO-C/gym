# # # =============================================
# # CONFIGURACIÓN DE BASE DE DATOS - MySQL
# # =============================================
#spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3307/gym_db?useSSL=false&serverTimezone=America/Lima&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true&rewriteBatchedStatements=true&cachePrepStmts=true&prepStmtCacheSize=250&prepStmtCacheSqlLimit=2048&useServerPrepStmts=true}
#spring.datasource.username=${DB_USER:root}
#spring.datasource.password=${DB_PASS:}
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# =============================================
# CONFIGURACIÓN DE HIKARICP (Connection Pool)
# =============================================
# Conexiones mínimas siempre abiertas (evita reconectar en cada petición)
spring.datasource.hikari.minimum-idle=5
# Máximo de conexiones simultáneas
spring.datasource.hikari.maximum-pool-size=20
# Tiempo máximo de espera para obtener una conexión (ms)
spring.datasource.hikari.connection-timeout=20000
# Tiempo máximo de inactividad antes de cerrar una conexión idle
spring.datasource.hikari.idle-timeout=300000
# Tiempo máximo de vida de una conexión (10 min)
spring.datasource.hikari.max-lifetime=600000
# Keepalive para evitar que el servidor cierre conexiones inactivas
spring.datasource.hikari.keepalive-time=60000
# Nombre del pool
spring.datasource.hikari.pool-name=GymHikariPool

# base de datos en el servidor 
spring.datasource.url=jdbc:mysql://190.116.26.62:3307/gym_db?useSSL=false&serverTimezone=America/Lima&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true&rewriteBatchedStatements=true&cachePrepStmts=true&prepStmtCacheSize=250&prepStmtCacheSqlLimit=2048&useServerPrepStmts=true
spring.datasource.username=root
spring.datasource.password=MUwjL1I5Cpc9dcGfdU
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# =============================================
# CONFIGURACIÓN DE JPA / HIBERNATE
# =============================================
# PRODUCCIÓN: Usar 'validate' o 'none' para evitar alteraciones no controladas del esquema.
# En desarrollo local puede usarse 'update'.
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
# open-in-view=true: necesario para cargar relaciones lazy en la respuesta JSON
spring.jpa.open-in-view=true
# Batch size para reducir número de queries en colecciones
spring.jpa.properties.hibernate.default_batch_fetch_size=20
spring.jpa.properties.hibernate.jdbc.batch_size=20

# Logging: solo errores críticos (reducir overhead de logging)
logging.level.com.gym=WARN
logging.level.org.hibernate.SQL=OFF
logging.level.org.springframework.web=ERROR
logging.level.org.springframework.web.servlet.mvc.method.annotation=ERROR

# =============================================
# CONFIGURACIÓN DEL SERVIDOR
# =============================================
server.port=${PORT:8080}
server.servlet.context-path=/api

# =============================================
# CONFIGURACIÓN DE LA APLICACIÓN
# =============================================
spring.application.name=gym-management

# =============================================
# CONFIGURACIÓN DE FACTURACIÓN (miapi.cloud)
# =============================================
facturacion.api.url=${FACTURACION_API_URL:https://miapi.cloud/apifact/invoice/create}
facturacion.api.key=${FACTURACION_API_KEY:miap-bjx-t0v-mqf}
facturacion.ruc.emisor=${FACTURACION_RUC_EMISOR:20000000001}

# =============================================
# CONFIGURACIÓN DE CONSULTAS (miapi.cloud DNI/RUC)
# =============================================
consulta.api.dni=${CONSULTA_API_DNI:https://miapi.cloud/v1/dni/}
consulta.api.ruc=${CONSULTA_API_RUC:https://miapi.cloud/v1/ruc/}
# SEGURIDAD: Este token debe moverse a variables de entorno antes de subir a produccion final
consulta.api.token=${CONSULTA_API_TOKEN:eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ1c2VyX2lkIjo2MDMsImV4cCI6MTc2NDY4NDM1N30.nH31PRzhb_PF61yLGccnjkkA1ajNZ8jJAKPVwpHL8tA}
rate.limit.max.requests=${RATE_LIMIT_MAX_REQUESTS:5}
rate.limit.window.seconds=${RATE_LIMIT_WINDOW_SECONDS:60}

# =============================================
# CONFIGURACIÓN DE CORREO - Brevo SMTP
# =============================================
spring.mail.host=smtp-relay.brevo.com
spring.mail.port=2525
spring.mail.username=aa7257001@smtp-brevo.com
spring.mail.password=Rg5mwKvaEfW97Dbp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.test-connection=false
spring.mail.properties.mail.smtp.ssl.trust=*
spring.mail.properties.mail.smtp.ssl.checkserveridentity=false

# IMPORTANTE: Coloca aquí el correo electrónico con el que te registraste en Brevo.
gym.mail.from=jhimitarrilllo7@gmail.com

# =============================================
# CONFIGURACIÓN DE MULTIPART (Subida de archivos)
# =============================================
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB